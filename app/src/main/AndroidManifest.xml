<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:theme"
        android:theme="@style/Theme.IstriaEvents.NoActionBar">
        <activity
            android:name="com.siljan.istriaevents.ui.onboarding.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name="com.siljan.istriaevents.ui.splash.SplashScreenActivity"
            android:theme="@style/Theme.IstriaEvents.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.siljan.istriaevents.MainActivity"
            android:exported="false" />
    </application>

</manifest>